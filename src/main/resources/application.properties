# Azure AD Configuration - these will be loaded from environment variables
azure.tenant.id=${AZ_TENANT_ID}
azure.client.id=${AZ_CLIENT_ID}
azure.client.secret=${AZ_CLIENT_SECRET}

# Dataverse Configuration
dataverse.org.url=${DV_ORG_URL}
dataverse.api.version=v9.2