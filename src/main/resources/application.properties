# Azure AD Configuration - loaded from environment variables
azure.tenant.id=${AZ_TENANT_ID}
azure.client.id=${AZ_CLIENT_ID}
azure.client.secret=${AZ_CLIENT_SECRET}

# Dataverse Configuration
dataverse.org.url=${DV_ORG_URL}
dataverse.api.version=v9.2

# Connection String Template (for reference)
# AuthType=ClientSecret;Url=${DV_ORG_URL};ClientId=${AZ_CLIENT_ID};ClientSecret=${AZ_CLIENT_SECRET};Authority=https://login.microsoftonline.com/${AZ_TENANT_ID}